<!DOCTYPE html>
<html class="h-full bg-indigo-950">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Sign in - Meridian Magazine Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="h-full flex items-center justify-center">
  <div class="w-full max-w-md px-6">
    <div class="text-center mb-8">
      <h1 class="text-3xl font-bold text-white">Meridian Magazine</h1>
      <p class="mt-2 text-indigo-300">IronAdmin Demo</p>
      <p class="mt-1 text-indigo-400 text-sm">Choose a role to explore the admin panel</p>
    </div>

    <div class="space-y-3">
      <% @users.each do |user| %>
        <%= form_tag("/login", method: :post) do %>
          <%= hidden_field_tag :user_id, user.id %>
          <button type="submit" class="w-full flex items-center gap-4 p-4 rounded-xl bg-white/10 hover:bg-white/20 border border-white/10 transition-all duration-200 text-left group">
            <div class="flex-shrink-0 w-12 h-12 rounded-full flex items-center justify-center text-lg font-bold
              <% if user.admin? %>bg-red-500/20 text-red-300<% elsif user.editor? %>bg-purple-500/20 text-purple-300<% else %>bg-blue-500/20 text-blue-300<% end %>">
              <%= user.name.chars.first %>
            </div>
            <div class="flex-1 min-w-0">
              <div class="text-white font-semibold group-hover:text-indigo-200"><%= user.name %></div>
              <div class="text-indigo-400 text-sm truncate"><%= user.email %></div>
            </div>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium
              <% if user.admin? %>bg-red-500/20 text-red-300
              <% elsif user.editor? %>bg-purple-500/20 text-purple-300
              <% else %>bg-blue-500/20 text-blue-300<% end %>">
              <%= user.role %>
            </span>
          </button>
        <% end %>
      <% end %>
    </div>

    <div class="mt-6 p-4 rounded-xl bg-white/5 border border-white/10">
      <p class="text-indigo-300 text-xs font-medium mb-2">About this demo</p>
      <ul class="text-indigo-400 text-xs space-y-1">
        <li>Each role has different permissions &mdash; try all three.</li>
        <li>Data resets periodically, so feel free to experiment.</li>
      </ul>
    </div>

    <p class="mt-6 text-center">
      <a href="https://github.com/rubylab-app/iron_admin" target="_blank"
         class="text-indigo-400 hover:text-indigo-300 text-xs transition-colors">
        View IronAdmin on GitHub &rarr;
      </a>
    </p>
  </div>
</body>
</html>
